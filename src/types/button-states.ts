export enum ButtonState {
    DEFAULT = 'default',
    PROGRESS = 'progress',
    COMPLETED = 'completed'
}

export enum MintButtonState {
    DEFAULT = 'default',
    MINTING = 'minting',
    MINTED = 'minted'
}
